# YWD CAN‑FD 智能电机控制协议 V0\.1（草案）

## 目录

1. 范围与术语
2. 物理层与链路层
3. CAN 标识符映射
4. 字段编码\(缩放整数\)
5. 控制模式与单电机帧
6. 多电机聚合帧
7. 反馈帧与发送调度
8. 参数访问\(块读/写\)
9. 寄存器映射
10. 系统命令
11. 多电机驱动器\(一个MCU,多个电机\)

## 1 范围与术语

### 1\.1 角色

- **主机\(master\)**：每条CAN‑FD 总线段上唯一控制器，发起控制/参数命令，接收反馈与响应
- **节点\(Node\)**：7位NODE\_ID 唯一标识逻辑电机
- **驱动器\(Driver\)**：单MCU可承载1个或多个逻辑电机

### 1\.2 术语表

| 术语                      | 说明                                |
| ------------------------- | ----------------------------------- |
| 总线段\(Segment\)         | 单路CAN‑FD双绞线总线，两端终端电阻 |
| Node                      | 7位NODE标识逻辑电机                 |
| Frame                     | 单条CAN‑FD报文                     |
| 控制帧\(Control frame\)   | 主机→驱动器，下发运动给定          |
| 聚合帧\(Aggregate frame\) | 广播帧，单帧最多4台电机指令         |
| 反馈帧\(Feedback frame\)  | 驱动器→主机，状态遥测报文          |
| RID                       | 8位寄存器地址ID                     |
| LSB                       | 缩放字段最小单位值                  |

### 1\.3 通用约定

1. 所有多字节数值**小端序**；
2. `B<n>` 代表报文第n字节（从0开始）；位区间 `[msb:lsb]`；
3. 十六进制带`0x`前缀；
4. Reserved保留位/字节发送填0，接收直接忽略。

### 1\.4 协议核心特性

- 单总线最多8个电机节点；
- 支持≥1kHz 实时控制周期；
- 三种闭环模式：MIT阻抗、位置速度梯形、恒速模式；
- CAN‑FD 11bit标准ID，仲裁1Mbps / 数据5Mbps，最大64字节载荷；
- 寄存器批量读写（单次最多8个寄存器）；
- 单反馈帧集成位置/速度/力矩/双温度/母线电压；
- 聚合广播帧，一帧多电机下发。

### 1\.5 一致性与厂商扩展

#### 必需实现项

CAN ID分配、缩放编码、三种控制帧、反馈帧、寄存器块读写、系统使能/失能/清故障；
寄存器：NODE\_ID、CMD\_TIMEOUT、FB\_DIV、CTRL\_MODE、PMAX/VMAX/TMAX、软限制0x18\~0x1C、硬限制0x36\~0x3A、版本Bank 0x60。

#### 可选实现项

其余系统命令、额外环路增益、电机参数可写、扩展遥测。

#### 异常返回规则

1. 标准寄存器只读返回`RSTAT=0x00`、值0；
2. 未知RID读写返回`RSTAT=0x01`；
3. 不支持控制模式写入寄存器返回`RSTAT=0x03`；
4. 控制帧模式与驱动器当前模式不匹配 → 故障0x09，停机。

#### 厂商扩展范围

- 单电机控制ID预留：`0x280~0x3FF`
- 聚合广播ID预留：`0x004~0x07F`
- 自定义寄存器：`0x80~0xFF`；标准寄存器`0x00~0x7F`

## 2 物理层与链路层

### 2\.1 位时序固定参数

| 参数                    | 数值               |
| ----------------------- | ------------------ |
| 报文格式                | CAN‑FD 11位标准ID |
| 仲裁段波特率            | 1 Mbps             |
| 数据段波特率\(BRS开启\) | 5 Mbps             |
| 最大载荷                | 64 Byte            |
| 错误处理                | 标准CAN‑FD规范    |

规则：报文使用最小合法DLC；未使用尾部字节填充0。
合法DLC长度：0/8/12/16/20/24/32/48/64。

### 2\.2 节点ID规则

- NODE\_ID：7bit，取值`0x01 ~ 0x7F`；
- 推荐设备分配范围：`0x01 ~ 0x08`；
- `NODE_ID=0x00` 仅广播使用，不可分配电机。

## 3 CAN 标识符映射

规则：ID数值越小总线仲裁优先级越高；
主机下发 `0x000~0x5FF`；驱动器上传 `0x600~0x7FF`；
主机过滤器：`ID & 0x600 == 0x600` 接收所有电机反馈/参数响应。

### 3\.1 ID分配总表

\| ID范围 \| 传输方向 \| 帧类型 \| 说明 \|
\|\-\-\-\-\-\-\-\-\|\-\-\-\-\-\-\-\-\-\-\|\-\-\-\-\-\-\-\-\|
\| 0x000 \| H→D \| 保留 \| 最高优先级同步/紧急帧预留 \|
\| 0x001 \| H→D \| Aggregate MIT \| 广播多电机阻抗指令 \|
\| 0x002 \| H→D \| Aggregate Pos\+Vel \| 广播多电机位置梯形指令 \|
\| 0x003 \| H→D \| Aggregate ConstVel \| 广播多电机恒速指令 \|
\| 0x004\~0x07F \| H→D \| 预留聚合ID \| 厂商新增控制模式 \|
\| 0x080\~0x0FF \| H→D \| 高优先级预留 \| \|
\| 0x100 \| node \| H→D \| 单电机MIT \| 单电机阻抗控制 \|
\| 0x180 \| node \| H→D \| 单电机位置速度 \| 梯形轨迹 \|
\| 0x200 \| node \| H→D \| 单电机恒速 \| 斜坡速度控制 \|
\| 0x280\~0x3FF \| H→D \| 单电机模式预留 \| 厂商扩展闭环 \|
\| 0x400 \| node \| H→D \| 系统命令 \| 使能、失能、置零、故障清除、保存、复位、恢复默认值 \|
\| 0x480 \| node \| H→D \| 参数读写请求 \| 寄存器块读/写 \|
\| 0x500\~0x5FF \| H→D \| 主机下发预留 \| \|
\| 0x600 \| node \| D→H \| 反馈帧 \| 电机实时状态遥测 \|
\| 0x680 \| node \| D→H \| 参数响应 \| 寄存器读写应答 \|
\| 0x700\~0x7FF \| D→H \| 驱动器上传预留 \| \|

### 3\.2 总线寻址规则

1. 单主机总线架构；
2. 单电机ID：`基地址 | node_id`；聚合/0x400可广播node=0；
3. 驱动器过滤器：接收全部聚合0x000\~0x07F、0x400广播、自身单电机/参数ID。

## 4 字段编码（缩放整数）

分两类编码：**量程归一化（位置/速度/力矩）**、**固定LSB缩放（增益、温度、电压）**

### 4\.1 量程归一化公式

```Plaintext
编码 raw = round( x / XMAX * FS ), x 钳位 [-XMAX, XMAX]
解码 x = raw / FS * XMAX
```

| 字段     | 类型   | XMAX来源      | FS满量程 | 分辨率             |
| -------- | ------ | ------------- | -------- | ------------------ |
| 位置     | int32  | PMAX\(rad\)   | 2³¹−1 | PMAX /\(2³¹−1\) |
| 速度     | int16  | VMAX\(rad/s\) | 2¹⁵−1 | VMAX / 32767       |
| Vmax参数 | uint16 | VMAX\(rad/s\) | 2¹⁶−1 | VMAX / 65535       |
| 力矩前馈 | int16  | TMAX\(N·m\)  | 2¹⁵−1 | TMAX / 32767       |

### 4\.2 固定LSB缩放

| 字段          | 类型   | LSB单位   | 数值范围                        |
| ------------- | ------ | --------- | ------------------------------- |
| Kp            | uint16 | 0\.01     | 0\~ 655\.35                     |
| Kd            | uint16 | 0\.001    | 0\~ 65\.535                     |
| 加减速ACC/DEC | uint16 | 1 rad/s² | 0\~ 65535（0=使用寄存器默认值） |
| 温度          | int8   | 1 ℃      | \-128 \~ 127                    |
| 母线电压      | int16  | 0\.01 V   | ±327\.67 V                     |

寄存器原始值：float32/uint32/int32，4字节小端，无缩放。

### 4\.3 换算示例

速度25 rad/s，VMAX=50 rad/s

```Plaintext
raw = round(25/50 * 32767) = 16384 → 小端 0x40 0x00
解码：16384 / 32767 * 50 ≈ 25.001 rad/s
```

## 5 控制模式与单电机帧

### 5\.1 MIT / 阻抗模式 ID = `0x100 | node`

DLC=12，报文Body 12字节

| 字节     | 字段            | 类型   |
| -------- | --------------- | ------ |
| B0\~B3   | P\_des 目标位置 | int32  |
| B4\~B5   | V\_des 目标速度 | int16  |
| B6\~B7   | Kp 比例增益     | uint16 |
| B8\~B9   | Kd 微分增益     | uint16 |
| B10\~B11 | Tff 力矩前馈    | int16  |

力矩公式：
$tau = K_p (P_{des}-P) + K_d (V_{des}-V) + T_{ff}$
子模式：

- Kp=0、Kd\>0 → 阻尼速度控制
- Kp=Kd=0 → 纯力矩控制

### 5\.2 位置速度模式 ID = `0x180 | node`

Body有效10字节，DLC=12，B10/B11填充0

| 字节   | 字段              | 类型   |
| ------ | ----------------- | ------ |
| B0\~B3 | P\_des 目标位置   | int32  |
| B4\~B5 | Vmax 最大巡航速度 | uint16 |
| B6\~B7 | ACC 加速度        | uint16 |
| B8\~B9 | DEC 减速度        | uint16 |

ACC/DEC=0 读取寄存器默认加减速。

### 5\.3 恒速模式 ID = `0x200 | node`

Body有效6字节，DLC=8，B6/B7填充0

| 字节   | 字段            | 类型   |
| ------ | --------------- | ------ |
| B0\~B1 | V\_des 目标速度 | int16  |
| B2\~B3 | ACC 加速度      | uint16 |
| B4\~B5 | DEC 减速度      | uint16 |

### 5\.4 模式匹配规则

1. 驱动器寄存器`CTRL_MODE`为当前有效模式；
2. 收到不同模式单电机帧 → 故障`0x09`，立即关输出；
3. 聚合帧仅匹配本机NODE\_ID记录校验模式；无匹配记录忽略；
4. 切换模式必须电机失能，切换自动清空P\_des/V\_des/Tff/Kp/Kd。

## 6 多电机聚合帧

广播ID：0x001\(MIT\)/0x002\(位置\)/0x003\(恒速\)，单帧最多4台电机。

### 6\.1 报文布局

```Plaintext
Header(1B) + [NODE_ID(1B) + Body(N字节)] * N
```

Header字节：bit0\~2=记录数量N，bit3\~7保留0

单条记录长度：

- MIT：12B；位置速度：10B；恒速：6B
  总载荷 = 1 \+ N\*\(1\+单条Body长度\)

### 6\.2 执行规则

1. 驱动器遍历全部记录，仅匹配自身NODE\_ID执行；
2. 未出现的电机保持上一周期指令；
3. 重复NODE\_ID取第一条有效；
4. 反馈仍使用各电机独立0x600\|node帧。

## 7 反馈帧与发送调度

### 7\.1 反馈帧 ID=`0x600 | node`，固定DLC=16

| 字节     | 字段                       | 说明                   |
| -------- | -------------------------- | ---------------------- |
| B0       | STATE\[7:4\] / MODE\[3:0\] | 运行状态\+当前控制模式 |
| B1       | FAULT                      | 故障码                 |
| B2\~B5   | POS 归一化位置             | int32                  |
| B6\~B7   | VEL 归一化速度             | int16                  |
| B8\~B9   | TORQUE 输出力矩            | int16                  |
| B10      | T\_MOS MOS温度             | int8 ℃                |
| B11      | T\_ROTOR 电机绕组温度      | int8 ℃                |
| B12\~B13 | VBUS 母线电压              | int16 LSB=0\.01V       |
| B14      | SEQ 帧序号                 | 自增0\~255，丢包检测   |
| B15      | Reserved                   | 填0                    |

### 7\.2 STATE 状态码（B0高4位）

| 值    | 状态           |
| ----- | -------------- |
| 0     | 失能           |
| 1     | 使能           |
| 2     | 故障锁存       |
| 3     | 校准中\(预留\) |
| 4\~15 | 保留           |

MODE低4位：1=MIT，2=位置速度，3=恒速。

### 7\.3 FAULT 故障码

| 故障值     | 故障描述           |
| ---------- | ------------------ |
| 0x00       | 无故障             |
| 0x01       | 过压               |
| 0x02       | 欠压               |
| 0x03       | 过流               |
| 0x04       | MOS管过温          |
| 0x05       | 线圈/转子过温      |
| 0x06       | 通信丢失\(看门狗\) |
| 0x07       | 过载               |
| 0x08       | 编码器错误         |
| 0x09       | 模式不匹配         |
| 0x0A\~0xFF | 预留               |

故障触发：立即关功率输出，故障锁存，必须下发CLEAR\_FAULT清除。

### 7\.4 反馈调度规则（寄存器FB\_DIV）

| FB\_DIV | 发送策略                       |
| ------- | ------------------------------ |
| 0       | 关闭反馈，仅寄存器轮询读取遥测 |
| 1       | 每收到1条控制帧回复1帧（默认） |
| N\>1    | 每N条控制帧回复1帧，降采样     |

反馈触发源：主机下发控制报文，同步时序。

## 8 参数访问（寄存器块读写）

ID=`0x480 | node` 请求；`0x680 | node` 响应；单帧最多8个寄存器。

### 8\.1 请求帧结构

B0 CMD：0x01读 / 0x02写
B1 N：寄存器数量 1\~8
B2开始：多条记录

- READ单记录：1字节 RID
- WRITE单记录：1字节RID \+ 4字节小端float32值

### 8\.2 响应帧结构

B0 回显CMD；B1 寄存器数量N
每条记录：`RID(1B) + RSTAT(1B) + value(4B LE)`
读/写统一格式；写入返回当前寄存器值用于校验。
最大长度：2 \+ 8\*6 = 50字节，适配CAN\-FD单帧。

### 8\.3 RSTAT 状态码

| RSTAT | 含义                             |
| ----- | -------------------------------- |
| 0x00  | 操作成功                         |
| 0x01  | 未知RID                          |
| 0x02  | 只读寄存器禁止写入               |
| 0x03  | 写入值超出范围                   |
| 0x04  | 当前状态禁止写入（如使能写模式） |

## 9 寄存器映射

所有RID 8bit，每Bank 16个寄存器（0x10对齐）；数值统一32位小端。

### 9\.1 Bank 0x00 身份通信（0x00\~0x0F）

| RID  | 名称         | 类型   | 读写 | 说明                                |
| ---- | ------------ | ------ | ---- | ----------------------------------- |
| 0x00 | NODE\_ID     | uint32 | RW   | 电机节点ID 1\~127，写入立即生效     |
| 0x01 | CMD\_TIMEOUT | uint32 | RW   | 通信看门狗ms，0关闭；超时报0x06故障 |
| 0x02 | FB\_DIV      | uint32 | RW   | 反馈分频系数，默认1                 |

### 9\.2 Bank 0x10 控制配置\&软限制（0x10\~0x1F）

| RID  | 名称       | 类型    | 读写     |
| ---- | ---------- | ------- | -------- |
| 0x10 | CTRL\_MODE | uint32  | RW       |
| 0x11 | PMAX       | float32 | RW       |
| 0x12 | VMAX       | float32 | RW       |
| 0x13 | TMAX       | float32 | RW       |
| 0x14 | ACC        | float32 | RW       |
| 0x15 | DEC        | float32 | RW       |
| 0x18 | P\_SOFT    | float32 | RW       |
| 0x19 | V\_SOFT    | float32 | 速度软限 |
| 0x1A | T\_SOFT    | float32 | RW       |
| 0x1B | ACC\_SOFT  | float32 | RW       |
| 0x1C | DEC\_SOFT  | float32 | RW       |

规则：软限不可大于硬件硬限，超限写入RSTAT=0x03。

### 9\.3 Bank 0x20 三环PID增益（0x20\~0x2F）

| RID  | 名称    | 类型                   |
| ---- | ------- | ---------------------- |
| 0x20 | KP\_CUR | float32 电流环P        |
| 0x21 | KI\_CUR | float32 电流环I        |
| 0x22 | KD\_CUR | float32 电流环D        |
| 0x23 | KP\_SPD | float32 速度环P        |
| 0x24 | KI\_SPD | float32 速度环I        |
| 0x25 | KD\_SPD | float32 速度环D        |
| 0x26 | KP\_POS | float32 位置环P        |
| 0x27 | KI\_POS | float32 位置环I        |
| 0x28 | KD\_POS | float32 位置环D        |
| 0x29 | I\_BW   | float32 电流环带宽Hz   |
| 0x2A | V\_BW   | float32 速度滤波带宽Hz |

### 9\.4 Bank 0x30 硬件硬限（只读 0x30\~0x3F）

| RID  | 名称      | 类型                  |
| ---- | --------- | --------------------- |
| 0x30 | UV\_VALUE | float32 欠压阈值V     |
| 0x31 | OV\_VALUE | float32 过压阈值V     |
| 0x32 | OT\_COIL  | float32 线圈过温℃    |
| 0x33 | OT\_MOS   | float32 MOS过温℃     |
| 0x34 | OC\_VALUE | float32 过流比例0\~1  |
| 0x35 | OVERLOAD  | float32 过载阈值      |
| 0x36 | P\_HARD   | float32 位置硬限rad   |
| 0x37 | V\_HARD   | float32 速度硬限rad/s |
| 0x38 | T\_HARD   | float32 力矩硬限N·m  |
| 0x39 | ACC\_HARD | float32 加速度硬限    |
| 0x3A | DEC\_HARD | float32 减速度硬限    |

### 9\.5 Bank 0x40 电机本体参数（0x40\~0x4F）

一体化电机只读，通用驱动器可配置

| RID  | 名称                |
| ---- | ------------------- |
| 0x40 | NPP 极对数 uint32   |
| 0x41 | RS 相电阻 Ω        |
| 0x42 | LS 相电感 H         |
| 0x43 | FLUX 转子磁链 Wb    |
| 0x44 | GR 减速比           |
| 0x45 | KT 力矩常数 N·m/A  |
| 0x46 | GREF 减速器效率0\~1 |

### 9\.6 Bank 0x50 实时遥测（只读 0x50\~0x5F）

| RID  | 名称                         |
| ---- | ---------------------------- |
| 0x50 | ROTOR\_POS 折算转子位置rad   |
| 0x51 | OUT\_POS 输出轴编码器位置rad |
| 0x52 | VB 当前母线电压V             |
| 0x53 | T\_PCB 驱动板PCB温度℃       |
| 0x54 | T\_MOTOR 电机绕组温度℃      |
| 0x55 | IQ q轴实时电流A              |

### 9\.7 Bank 0x60 版本信息（只读 0x60\~0x66）

| RID  | 说明                         |
| ---- | ---------------------------- |
| 0x60 | PROTOCOL\_VER 固定0x00010000 |
| 0x61 | VENDOR\_ID 4字节ASCII厂商ID  |
| 0x62 | PRODUCT\_ID 产品型号ID       |
| 0x63 | HW\_VER 硬件版本             |
| 0x64 | SW\_VER 固件版本             |
| 0x65 | BOOT\_VER 引导程序版本       |
| 0x66 | SN 设备唯一序列号            |

### 9\.8 Bank 0x70 标准保留（0x70\~0x7F）

读取返回0，RSTAT=0；写入推荐RSTAT=0x02只读拒绝。

### 9\.9 厂商自定义寄存器 0x80\~0xFF

主机读取返回RSTAT=0x01未知RID，厂商私有功能。

## 10 系统命令

ID=`0x400 | node`；node=0为全局广播；DLC=1，仅B0子命令

| 子命令码 | 名称           | 功能                      |
| -------- | -------------- | ------------------------- |
| 0x01     | ENABLE         | 电机使能，开启闭环        |
| 0x02     | DISABLE        | 电机失能，关功率输出      |
| 0x03     | SET\_ZERO      | 当前轴置零点，清空给定    |
| 0x04     | CLEAR\_FAULT   | 故障消失后清除锁存故障    |
| 0x05     | SAVE           | 寄存器写入Flash（仅失能） |
| 0x06     | RESET          | 驱动器软复位              |
| 0x07     | LOAD\_DEFAULTS | 加载出厂默认RAM参数       |

执行系统命令后驱动器自动回复一帧反馈作为确认。

## 11 多电机驱动器（单MCU多节点）

1. 单个驱动MCU可承载多个NODE\_ID，每个电机独立寄存器、独立反馈帧；
2. 聚合帧可同时控制本机多台电机；
3. 每台电机独立FB\_DIV分频控制反馈发送频率；
4. 反馈报文按NODE\_ID从小到大依次发送，互不干扰；
5. 所有ID、报文格式、寄存器定义无任何改动。

```Plaintext
```

> （注：部分内容可能由 AI 生
